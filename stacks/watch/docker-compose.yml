services:
  jellyfin:
    image: jellyfin/jellyfin:latest
    container_name: jellyfin
    ports:
      - "${JELLYFIN_PORT}:8096"
    volumes:
      - ../../config/jellyfin:/config
      - ${DATA_PATH}/media/movies:/media/movies:ro
      - ${DATA_PATH}/media/shows:/media/shows:ro
    environment:
      - TZ=${TZ}
    networks:
      - media-net
    restart: unless-stopped

networks:
  media-net:
    external: true
